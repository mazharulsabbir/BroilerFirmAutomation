package tarmsbd.iot.automation.broilerfirm.ui.notification.viewmodel

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class NotificationViewModel constructor(application: Application) : AndroidViewModel(application) {
}